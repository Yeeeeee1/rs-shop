import { SearchWordPipe } from './search-word.pipe';

describe('SearchWordPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWordPipe();
    expect(pipe).toBeTruthy();
  });
});
